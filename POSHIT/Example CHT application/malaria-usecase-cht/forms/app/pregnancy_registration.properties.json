{
  "icon": "pregnancy-registration",
  "context": {
    "place": false,
    "person": true,
    "expression": "!contact.date_of_death && !summary.muted && ageInYears(contact) >= 12 && ageInYears(contact) <= 49 && contact.sex === 'female'"
  }
}