{
    "icon": "pregnancy-registration",
    "context": {
      "place": false,
      "person": false,
      "expression": "!contact.date_of_death && !summary.muted && ageInYears(contact) >= 12 && summary.is_active_pregnancy && ageInYears(contact) <= 49 && contact.sex === 'female'"
    }
  }