{
    "context": {    
        "person": true,
        "place": false,
        "expression": "!!contact && !contact.muted && !contact.date_of_death && summary.firstParentType === 'clinic'&& user.role === 'vht'"
    }
}
