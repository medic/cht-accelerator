{
    "context": {
      "person": true,
        "place": false,
        "expression": "contact.type === 'health_center' && user.parent && user.parent.type === 'health_center' && user.role === vht "
    }
  }
  
