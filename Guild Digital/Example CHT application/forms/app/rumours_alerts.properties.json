{
   "icon":"icon-alerts",
    "context": {
      "person": false,
      "place": true,
      "expression": "((contact.type === 'person' && summary.firstParentType === 'clinic') || contact.type === 'clinic') && !contact.muted && user._id !== contact._id"
    }
  }
  
