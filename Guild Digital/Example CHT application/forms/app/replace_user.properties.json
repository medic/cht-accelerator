{
    "icon": "icon-death-general",
    "title": "Replace VHT",
    "context": {
        "person": true,
        "place": false,
        "expression": "contact.type === 'person' && user.role === 'vht_supervisor' && !(contact.type === 'clinic')"
    }
}
