{
  "context": {
    "person": false,
    "place": true,
    "expression": "contact.type === 'health_center' && user.parent && user.parent.type === 'health_center'contact.type === 'health_center' && user.role === 'vht'"
  }
}
